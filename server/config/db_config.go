// Contains the Postgres Database configurations.

package config

const (
	DBUser     = `swarup`
	DBPassword = `wordpass`
	DBName     = `case_db`
	DBHost     = `localhost`
	// DBHost = `host.docker.internal`
	DBPort = `5432`
)
